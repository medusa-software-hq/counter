name: Check core service infra

on:
  workflow_call:

permissions:
  contents: read

env:
  TERRAFORM_VERSION: 1.14

  TF_INPUT: false
  TF_CLI_ARGS: -no-color
  NO_COLOR: 1

jobs:
  check-config:
    name: Check Terraform configuration
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ vars.GCP_CICD_WI_PROVIDER_NAME }}
          service_account: ${{ vars.GCP_CICD_SA_EMAIL }}

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: ~> ${{ env.TERRAFORM_VERSION }}

      - name: Check formatting
        working-directory: services/core-service/infra
        run: terraform fmt -check -recursive

      - name: Plan Terraform application
        working-directory: services/core-service/infra
        env:
          TF_VAR_gcp_project_id: ${{ vars.GCP_PROJECT_ID }}
        run: |
          terraform init
          terraform plan | tee terraform-plan.txt
          {
            echo '## Terraform plan'
            echo ''
            echo '```'
            cat terraform-plan.txt
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
